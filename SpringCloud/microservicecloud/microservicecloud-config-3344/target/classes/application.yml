server:
  port: 3344

spring:
  application:
    name: microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:wosiniDad/microservicecloud-config.git #GitHub上面的git仓库名称